version: '3.8'

services:
  db:
    build: .
    ports:
      - '8081:5432'
    environment:
      POSTGRES_USER: value
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: db
    restart: unless-stopped
    volumes: 
      - C:\Users\rzver\source\repos\ecommerce-app\build\data:/var/lib/postgresql/data


